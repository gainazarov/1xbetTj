# Telegram-бот 1xBet TJ — рассылки и WebView

Проект — Telegram-бот на **aiogram 3**, который:
- ведёт пользователей в WebView-сервис (сайт проекта),
- собирает и показывает статистику аудитории,
- позволяет администраторам делать массовые рассылки по базе,
- поддерживает **два варианта рассылок** (по ссылке и из постов канала),
- умеет **планировать (откладывать)** рассылки на нужное время,
- ведёт логи в файл и базу.

---

## Основной функционал

### Для обычного пользователя
- Команда `/start`:
  - приветственное сообщение с описанием возможностей бота и сервиса;
  - кнопка «Открыть сервис» (WebView) для перехода на внешний сайт.
- Reply-клавиатура:
  - кнопка «Играть», открывающая WebView по адресу `SITE_URL`.

### Для администратора
Полномочия определяются списком `ADMIN_IDS` из `.env`. Для администраторов доступны:

1. **Админ-панель (inline-меню)**
   - Кнопка «Админ-панель» в основном меню.
   - Внутри панели:
     - «Создать рассылку по ссылке»;
     - «Создать рассылку из постов»;
     - «Статистика»;
     - «Запланированные рассылки»;
     - «Закрыть».

2. **Постоянная admin reply-клавиатура**
   - «Играть» (WebView);
   - «📨 Рассылка по ссылке»;
   - «📨 Рассылка из постов»;
   - «📊 Статистика»;
   - «🕒 Запланированные рассылки».

3. **Рассылка по ссылке (вариант B)**
   - Админ отправляет боту ссылку на пост канала в формате `https://t.me/имя_канала/номер`.
   - Бот:
     - пробует разные варианты идентификатора канала (`@username`, `username`, numeric id),
     - показывает превью сообщения администратору,
     - предлагает выбрать тип рассылки,
     - даёт выбор: **отправить сейчас** или **запланировать**.

4. **Рассылка из постов (вариант A)**
   - Бот, будучи админом в канале, сохраняет новые посты в таблицу `channel_posts`.
   - Админ может открыть список последних постов (до 10 штук):
     - каждый пост отображается одной строкой с укороченным превью текста,
     - выбирается нужный пост,
     - затем — тип рассылки,
     - далее — отправка сразу или планирование.

5. **Типы рассылок**
   - Новости (`news`)
   - Акция (`promotion`)
   - Важное уведомление (`important_notification`)
   - Тестовая рассылка (`test_mailing`, только по администраторам)

6. **Планирование рассылок**
   - На этапе подтверждения админ может:
     - нажать «Запланировать»;
     - ввести дату и время отправки в формате `ДД.ММ.ГГГГ ЧЧ:ММ`.
   - Бот:
     - проверяет корректность формата и что время в будущем,
     - создаёт запись в таблице `scheduled_mailings` со статусом `pending`,
     - отвечает админу с подтверждением (время, ID задачи, тип, ссылка).
   - Фоновый воркер:
     - регулярно (каждые 30 секунд) проверяет, какие задачи пора выполнить,
     - запускает те, у которых `status = 'pending'` и `scheduled_at <= now`,
     - отправляет рассылку тем же кодом, что и «моментальная» рассылка,
     - обновляет статусы на `processing`, `done` или `failed`.

7. **Просмотр и отмена запланированных рассылок**
   - Кнопка «Запланированные рассылки» доступна:
     - в inline-админ-панели;
     - в нижней reply-клавиатуре админа.
   - При нажатии бот показывает список последних (до 10) задач:
     - ID,
     - тип,
     - дата/время отправки,
     - статус (ожидает / в процессе / отправлена / ошибка / отменена).
   - Для задач в статусе `pending` доступны inline-кнопки «Отменить ID …»,
     по которым статус меняется на `cancelled` и задача не будет отправлена.

8. **Статистика**
   - Статистика по пользователям:
     - общее количество;
     - новые за 24 часа;
     - активные за 24 часа / 7 дней / 30 дней;
     - количество пользователей, удаливших бота.
   - Статистика по рассылкам:
     - последние (до 5) рассылок;
     - по каждой: тип, доставлено из скольких, количество ошибок.

9. **Логирование и аналитика WebView**
   - Все ключевые события логируются в `bot.log` (с ротацией файлов).
   - События открытия WebView сохраняются в таблицу `webview_events`.
   - Ошибки отправки сообщений и рассылок регистрируются через вспомогательную функцию
     с указанием `user_id`, контекста и текста ошибки.

---

## Архитектура проекта

Основные файлы и модули:

- `main.py`
  - точка входа в приложение;
  - инициализация БД, `Bot`, `Dispatcher` и FSM-хранилища;
  - подключение роутеров (`handlers_start`, `handlers_admin`, `handlers_mailings`, `handlers_channel`);
  - запуск long polling;
  - запуск фонового воркера для запланированных рассылок.

- `config.py`
  - загрузка переменных окружения через `python-dotenv`;
  - константы: `BOT_TOKEN`, `SITE_URL`, `ADMIN_IDS`, `DB_PATH`, `LOG_FILE`;
  - функция `is_admin(user_id)`;
  - настройка логирования (консоль + `RotatingFileHandler` для `LOG_FILE`).

- `db.py`
  - функции работы с SQLite-базой:
    - создание и миграция таблиц: `users`, `mailings`, `scheduled_mailings`, `webview_events`, `channel_posts`;
    - операции по пользователям (`upsert_user`, `mark_user_blocked`, выборка активных и админов);
    - создание и обновление рассылок (`create_mailing`, `update_mailing_counters`, `get_recent_mailings`);
    - аналитика (`get_user_stats`);
    - запланированные рассылки (`create_scheduled_mailing`, `get_due_scheduled_mailings`, `get_scheduled_mailings`, `update_scheduled_mailing_status`);
    - работа с постами канала (`save_channel_post`, `get_recent_channel_posts`).

- `states.py`
  - описание состояний FSM для админ-панели и рассылок (`AdminStates`).

- `keyboards.py`
  - генерация всех inline- и reply-клавиатур:
    - основное меню;
    - админ-панель;
    - выбор типа рассылки;
    - подтверждение (отправить сейчас / запланировать / отмена);
    - список постов канала;
    - пользовательская и админская reply-клавиатуры.

- `constants.py`
  - текстовые константы для кнопок админской reply-клавиатуры.

- `handlers_start.py`
  - обработка `/start`;
  - показ основного меню;
  - вход в админ-панель;
  - открытие WebView.

- `handlers_admin.py`
  - вся логика админ-панели и статистики;
  - просмотр и отмена запланированных рассылок (через inline и reply-интерфейсы).

- `handlers_mailings.py`
  - логика рассылок:
    - по ссылке;
    - из постов канала;
    - выбор типа;
    - подтверждение/отмена;
    - планирование;
    - отправка рассылки в фоне.
  - фоновый worker `scheduled_mailings_worker`, который проверяет и выполняет запланированные рассылки.

- `handlers_channel.py`
  - обработка сообщений из канала, сохранение постов для варианта A (рассылка из списка постов).

- `logger_utils.py`
  - вспомогательная функция для единообразного логирования ошибок с контекстом и `user_id`.

---

## Используемые библиотеки

Основные внешние зависимости (см. `requirements.txt`):

- **aiogram (>= 3.0.0)**
  - современный асинхронный фреймворк для Telegram-ботов;
  - используется для:
    - обработки апдейтов (Dispatcher, Router);
    - FSM (StatesGroup, FSMContext);
    - callback-кнопок, inline- и reply-клавиатур;
    - обработки ошибок Telegram API (например, `TelegramRetryAfter`).

- **python-dotenv (>= 1.0.0)**
  - загрузка конфигурации и секретов из файла `.env`;
  - удобная локальная разработка без захардкоженных токенов.

В стандартной библиотеке активно используются:
- `asyncio` — для фоновой задачи планировщика и ожидания между отправками;
- `sqlite3` — хранения данных бота;
- `logging` — логирование в консоль и файл;
- `datetime` — время и даты для статистики и планировщика.

---

## Файл .env — настройка окружения

В корне проекта должен быть файл `.env` со значениями, например:

```bash
BOT_TOKEN=1234567890:AA...your_token_here
SITE_URL=https://example.com
ADMIN_IDS=111111111,222222222
DB_PATH=bot.db
LOG_FILE=bot.log
```

- `BOT_TOKEN` — токен бота от @BotFather (обязательно);
- `SITE_URL` — URL WebView-сайта (желательно `https`);
- `ADMIN_IDS` — список Telegram ID администраторов через запятую или точку с запятой;
- `DB_PATH` — путь к файлу SQLite-базы;
- `LOG_FILE` — путь к файлу логов.

---

## Запуск проекта (локально / на сервере)

1. **Клонировать репозиторий и перейти в директорию проекта**

```bash
git clone https://github.com/gainazarov/1xbetTj.git
cd 1xbet
```

2. **Создать виртуальное окружение и установить зависимости**

```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate    # Windows

pip install -r requirements.txt
```

3. **Создать и заполнить `.env`** (см. пример выше).

4. **Запустить бота**

```bash
python main.py
```

5. **Рекомендации для production**

- Использовать процесс-менеджер (systemd, supervisor, pm2, Docker и т.п.), чтобы бот
  автоматически перезапускался при сбоях.
- Держать `.env` вне репозитория или защищённым, не публиковать `BOT_TOKEN`.
- Убедиться, что бот добавлен администратором в нужные каналы с необходимыми правами
  (чтение постов и отправка сообщений).

---

## Контакты

Свяэитесь с разработчиком для связи по вопросам бота и проекта.

- Telegram: `https://t.me/gainazarov_a`
- Email: `ardasher.gainazarov@mail.ru`
- Сайт / лендинг проекта: `https://gainazarov.com`
